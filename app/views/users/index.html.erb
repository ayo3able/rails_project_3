
   <h4><%= @user.email %></h4>
    <div class="card-deck">
 <%# <% @projects.each do |project| %> 
  <div class="card-header">
    <ul class="nav nav-pills card-header-pills">
      <li class="nav-item">
        <a class="nav-link btn btn-primary"><%# <%= link_to 'edit', edit_project_path(project) %>Edit</a> 
      </li>
      <br/>
      <li class="nav-item">
        <a class="nav-link btn btn-primary"><%# <%= link_to 'show', project %>See Task</a> 
      </li>
    </ul>
  </div>
  <div class="card-body">
     <h5 class="card-title"><%#<%= project.title %></h5> 
     <p class="card-text"><%#<%= project.goal %></p> 
   
    <div class="card-footer bg-transparent border-success"></div>
      <a class="btn btn-alarm"><%#<%= link_to 'delete', project, method: :delete, data: { confirm: 'Are you sure?' }%>Delete</a> 
  </div>
</div>
    <%# <% end %> 
</div>
